# Basic Python Chatbot

This repository includes a minimal command-line chatbot written in Python.

## Run it

```bash
python3 chatbot.py
```

## What it does

- Responds to greetings (`hi`, `hello`, `hey`)
- Answers a simple name/help question
- Echoes any other message
- Exits when you type `quit` or `exit`
